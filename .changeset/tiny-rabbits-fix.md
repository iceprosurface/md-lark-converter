---
---

Align the release workflow with the repository's pnpm 9 lockfile so frozen installs succeed in GitHub Actions.

Also document the Changesets-based release flow and the move to npm trusted publishing with OIDC.
